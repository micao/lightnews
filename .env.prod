DEBUG=False
SECRET_KEY=你的生产环境强随机密钥_建议至少50位
ALLOWED_HOSTS=example.com 你的服务器IP

# 数据库配置
DB_NAME=my_prod_db
DB_USER=prod_user
DB_PASSWORD=你修改后的强密码
DB_PORT_EXPOSE=15432  # 生产环境中不向外直接暴露默认5432端口

# 容器端口暴露与 Nginx 映射
WEB_PORT_EXPOSE=80    # 生产环境直接走标准80（或443）
NGINX_CONFIG=prod.conf
